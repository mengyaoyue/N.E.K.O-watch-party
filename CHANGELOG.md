# 更新记录

## v0.7.0

- 新增：**思考模式开关**（`thinking_mode`，面板可改）——模型默认会「先想一大段再回答」，这段思考很吃时间；关掉后能明显缩短「截屏 → 出话」的延迟
- 细粒度：可分别关闭「说话」与「看画面」两条链路的思考，共四档：`all` 都关（默认，最快）/ `chat` 只关出话 / `vision` 只关看画面 / `none` 都保留
- 对话链路：请求体顶层写入 `thinking:{type:"disabled"}`（DeepSeek 官方字段）；模型不认这个字段时自动退回普通请求
- 视觉链路：以 `extra_body` 透传同样的字段；宿主视觉接口不接受参数时**自动退回原样重试一次**，绝不因为提速而看不了画面
- 面板：新增「思考模式」选择器，会话状态里也会显示当前档位

## v0.6.0

- 移除：**播放进度 / 进度条 / 时间点** 相关的一切（`_current_position`、面板进度条、自动对齐 `_auto_align`、跳转 `_jump`、`extract_playback_time`、`fmt_pos`）——它没有上限、时间还能超过视频时长，是明显的幻觉来源
- 重构：陪看改为**开关制**——对猫娘说「陪我看」或在面板点「打开陪看开关」；开关打开后按设定节奏**高频自主截屏、主动评论**（抓笑点 / 梗 / 名场面），开关关闭则安静
- 新增：待机自动探测（`auto_detect`）：低频截屏判断主人是否在看视频，发现即自动打开开关
- 新增：双档截屏节奏（`shot_interval_active` 在看 / `shot_interval_idle` 待机），面板可自定义
- 调整：**预学习改为可选辅助**（`prelearn`，默认关）；不开也能陪看，猫娘纯靠截屏画面理解
- 大改：管理面板整体重做（开关 + 截屏频率 + 功能开关，去掉进度条 / 跳转 / 自动对齐）

## v0.5.0

- 新增：面板截屏频率控制（可调 10~600 秒，持久化保存）
- 新增：宿主语音输入检测，语音开启时自动切高频陪看
- 新增：播放门禁，无真实播放时暂停发稿
- 新增：SQLite 时间轴数据库，记录陪看历史
- 新增：非共享时低频截屏模式（默认 180 秒），共享时高频
- 优化：屏幕共享状态变化时即时响应
- 优化：配置改为 profiles 机制管理
- 修复：若干稳定性问题

## v0.4.0

- 新增：同步播放窗口，直接读取播放器进度
- 新增：面板内 B 站登录，自动获取 SESSDATA
- 新增：视觉模型看画面（支持 DeepSeek 等任意厂商）
- 新增：自动对齐，心跳时截屏读进度条校准

## v0.3.5

- 新增：截屏辅助，本地 OCR 读取画面文字
- 新增：反应密度面板自定义
- 新增：三级数据链抗风控

## v0.3.0

- 初始版本：预习、陪看、心跳、总结
